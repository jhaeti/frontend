import { NextPage } from "next/types";
import Form from "../components/Form"

const form : NextPage = () => {
    return (
        <div>
            <Form />
        </div>
    );
}

export default form;